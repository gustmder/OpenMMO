# 맵 및 지형 시스템 설계 (Map & Terrain Design)

이 문서는 OnlineRPG의 거대 오픈월드 구현을 위한 지형 시스템과 맵 에디터의 기술적 사양을 정의한다.

## 1. 세계 규모 및 해상도 (World Scale)

*   **전체 월드 크기:** 32km x 32km (또는 64km x 32km 확장 가능)
    *   약 1,024 $km^2$ 규모의 심리스(Seamless) 오픈월드 지향.
*   **지형 해상도:** 1m/px (Grid-based)
    *   하이트맵(Heightmap)의 한 픽셀이 실제 게임 월드의 1m x 1m 면적을 담당한다.
    *   캐릭터의 이동과 상호작용은 이 1m 그리드를 기준으로 이루어진다.
    *   0.5m 해상도 대비 1m를 선택한 이유:
        *   0.5m로 올리면 타일당 버텍스 4배(4K→16K), 삼각형 4배(8K→32K), 저장 4배(8KB→32KB)로 비용이 급증한다.
        *   하이트맵은 XZ 좌표당 높이 값이 하나뿐이므로, 해상도를 올려도 수직 절벽이나 오버행(돌출 지형)은 표현할 수 없다.
        *   절벽, 동굴, 바위벽 등은 별도 3D 오브젝트 메쉬를 배치하여 표현한다.
*   **다층 지형 처리:**
    *   하이트맵은 XZ 좌표당 높이가 하나뿐이므로 다리, 2층 구조, 터널 등 수직으로 겹치는 지형은 표현할 수 없다.
    *   이러한 구조물은 별도 3D 오브젝트 메쉬로 배치하며, 하이트맵은 그 아래의 기반 지형(강바닥, 지면 등)을 담당한다.
    *   충돌 처리: 오브젝트에 "걸을 수 있는 높이" 속성을 부여하여, 플레이어가 오브젝트 위에 있으면 오브젝트 높이를, 아래에 있으면 하이트맵 높이를 사용한다.
*   **하이트맵 데이터 사양:**
    *   **데이터 타입:** uint16 (2바이트/픽셀)
    *   **해석:** `height = value * 0.05 - 500.0` (미터 단위)
    *   **높이 범위:** -500.0m ~ +2,776.75m (0.05m 정밀도)
    *   **해수면 기준:** value = 10000 → 0.0m
    *   **타일당 저장 크기:** 65 x 65 = 8,450바이트 (버텍스 기반, 인접 타일과 가장자리 버텍스 공유)
        *   64x64(셀 기반)로 저장하면 타일 경계의 높이를 구할 때마다 인접 타일을 로드해야 하지만, 65x65로 가장자리 버텍스를 중복 저장하면 단일 타일만으로 모든 버텍스 높이를 즉시 참조할 수 있어 더 효율적이다.
    *   **파일:** `data/terrain/height/r{±xx}_{±zz}/h_{±xxxxx}_{±zzzzz}.bin`

## 2. 타일링 및 로딩 시스템 (Tiling & Loading)

*   **타일 크기 (Tile Size):** 64m x 64m
    *   전체 월드는 $500 \times 500$ (32km 기준) 개의 타일로 분할 관리된다.
    *   클라이언트 렌더링 및 하이트 데이터 관리의 최소 단위.
*   **세그먼트 해상도:** 64 x 64 Quads
    *   타일 하나당 $64 \times 64$개의 격자 메쉬를 생성하여 1m 해상도를 유지한다.
*   **로딩 전략:** 3x3 동적 스트리밍
    *   플레이어가 위치한 중앙 타일을 포함하여 주변 3x3 (총 9개) 타일을 메모리에 로드한다.
    *   쿼터뷰(Isometric) 특성상 화면에 노출되는 타일은 최대 4개이므로, 3x3 로딩으로 충분한 시야와 심리스 이동을 보장한다.

## 3. 지형 표현 및 렌더링 (Terrain Rendering)

*   **V2 Palette Splatting (인덱스 기반 2-텍스처 블렌드):** 상세는 [SPLATMAP_V2.md](SPLATMAP_V2.md).
    *   리전당 **최대 16개** 텍스처 팔레트를 구성하고, 각 셀은 팔레트에서 **2개**를 골라 블렌드한다.
    *   셀당 4바이트: `byte 0` = `(primaryIdx << 4) | secondaryIdx`, `byte 1` = reserved, `byte 2` = blend(0=primary, 255=secondary), `byte 3` = vegMeta(풀 밀도/subtype).
    *   **스플랫맵 데이터 사양:**
        *   **데이터 타입:** uint8 × 4바이트/셀 (V2 인코딩)
        *   **해상도:** 64 x 64 (셀 기반, 하이트맵의 버텍스 기반과 다름)
        *   **타일당 저장 크기:** 64 x 64 x 4 = 16,384바이트
        *   **필터링:** NEAREST (인덱스 바이트는 보간 불가)
        *   **파일:** `data/terrain/splat/r{±xx}_{±zz}/s_{±xxxxx}_{±zzzzz}.bin`
*   **리전 메타데이터:** 리전(16x16 타일 = 1km x 1km) 단위로 팔레트를 JSON으로 관리한다.
    *   `layers` 배열은 **1..16개** 항목. 각 항목은 `texture`와 `tileScale`.
    *   파일: `data/terrain/meta/r{±xx}_{±zz}.json`
    *   예시 (해안+산악 리전):
        ```json
        {
          "layers": [
            { "texture": "rocky_terrain_02_1k", "tileScale": 8.0 },
            { "texture": "sandy_gravel_02_1k",  "tileScale": 8.0 },
            { "texture": "red_laterite_soil_stones_1k", "tileScale": 10.0 },
            { "texture": "snow_02_1k", "tileScale": 4.0 },
            { "texture": "patterned_paving_02_1k", "tileScale": 30.0 },
            { "texture": "gravel_road_1k", "tileScale": 8.0 }
          ]
        }
        ```
    *   **Atlas**: 각 리전의 팔레트는 클라이언트에서 4×4 atlas (512px 슬롯)로 패킹되어 GPU에 업로드된다.
    *   *(TODO)* 리전 경계에서 텍스처 전환이 발생할 수 있다. 경계 블렌딩은 향후 확장으로 처리한다.
*   **PBR 데이터:** 각 레이어는 Albedo, Normal, ORM(AO, Roughness, Metallic) 데이터를 포함하여 실시간 조명에 반응한다.
    *   텍스처 데이터는 `.glb` 형식으로 `client/public/textures/` 폴더에 저장되어 있다.
*   **시각화 가이드 (Debug Grid):**
    *   **1m 그리드:** 검은색 선으로 표시 (편집 기준점).
    *   **64m 그리드:** 빨간색 선으로 표시 (타일 경계선).

## 4. 맵 에디터 핵심 기능 (Editor Features)

지형 편집기는 클라이언트 내부에 "에디터 모드"로 구현하여 실시간 피드백을 극대화한다.

1.  **지형 높이 조절 (Height Painter):**
    *   마우스 클릭/드래그로 지형의 높낮이(Y축)를 조절.
    *   브러시 크기 및 강도(Falloff) 지원.
2.  **지형 질감 칠하기 (Splat Painter):**
    *   4가지 텍스처 레이어를 브러시로 직접 칠하여 지형의 질감을 결정.
    *   RGBA 가중치 조절을 통한 부드러운 전환 처리.
3.  **오브젝트 배치 (Object Placement):**
    *   나무, 바위, 집 등 정적 오브젝트를 그리드 단위로 배치.
    *   타일당 JSON 파일로 저장한다.
        *   파일: `data/terrain/objects/r+00_+00/o_+0000_+0000.json`
        *   예시:
            ```json
            {
              "objects": [
                { "type": "oak_tree_01", "x": 12, "z": 34, "rot": 90 },
                { "type": "stone_house_01", "x": 5, "z": 10, "rot": 0, "scale": 1.5 }
              ]
            }
            ```
        *   `x`, `z`는 타일 내 로컬 셀 좌표 (0~63). `rot`은 Y축 회전(도). `scale`은 생략 시 1.0.
    *   **NPC 스폰 포인트:** 에디터에서 NPC 스폰 위치를 셀 단위로 배치한다. 타일 오브젝트 파일의 `npcs` 배열에 저장.
        *   스폰 데이터: `{ "type": "weapon_merchant", "x": 6, "z": 10, "rot": 180 }`
        *   NPC 타입 정의(행동, 대화, 상점 등)는 게임 콘텐츠 시스템에서 별도 관리.
    *   **밀집 장식 (잔디, 꽃 등):** 스플랫맵 가중치 기반으로 절차적(procedural) 생성한다. 별도 저장 불필요.
        *   예: R 채널(풀 지형) 가중치가 높은 셀에 잔디 인스턴스를 자동 배치.
    *   **오브젝트 타입 정의 (Object Definitions):** 타입별 메타데이터를 JSON 카탈로그로 관리한다.
        *   파일: `data/terrain/object_defs/{type_name}.json`
        *   주요 필드:
            *   `mesh`: GLB 모델 경로
            *   `category`: 분류 (tree, building, structure, rock, decoration 등)
            *   `size`: 셀 단위 바운딩 `[x, z]`
            *   `walkBlock`: 지상 이동 차단 셀 목록 (오브젝트 로컬 좌표) → 배치 시 이동 플래그 맵에 자동 반영
            *   `layer`: 다층 구조물용. `walkHeight`(위를 걸을 수 있는 높이), `walkCells`(위에서 걸을 수 있는 셀 목록). 단층 오브젝트는 `null`.
            *   `interior`: 실내 진입 시 가시성 제어. 건물 GLB 하나에 모든 파트를 포함하되, 메쉬 이름으로 런타임 분리.
                *   `occluderMeshNames`: 카메라를 가리는 메쉬 이름 목록 (지붕, 앞벽 등). 진입 시 숨김/반투명 처리.
                *   `enterCells`: 출입구 셀 좌표 (오브젝트 로컬).
                *   `innerCells`: 내부 셀 좌표. 플레이어가 이 셀에 진입하면 occluder를 숨긴다.
                *   실내가 없는 오브젝트는 `null`.
        *   예시 (건물):
            ```json
            {
              "mesh": "models/objects/stone_house_01.glb",
              "category": "building",
              "size": [3, 3],
              "walkBlock": [[0,0],[1,0],[2,0],[0,2],[2,2]],
              "layer": null,
              "interior": {
                "occluderMeshNames": ["Roof", "WallFront"],
                "enterCells": [[1,0]],
                "innerCells": [[1,1]]
              }
            }
            ```
        *   예시 (다리):
            ```json
            {
              "mesh": "models/objects/wooden_bridge_01.glb",
              "category": "structure",
              "size": [2, 6],
              "walkBlock": [],
              "layer": { "walkHeight": 3.0, "walkCells": [[0,0],[1,0],[0,1],[1,1]] },
              "interior": null
            }
            ```
        *   정적 데이터이므로 클라이언트 시작 시 전체 로드하거나, 리전 진입 시 필요한 것만 lazy 로드.
4.  **데이터 저장 및 관리:**
    *   타일 데이터는 헤더 없는 raw 바이트 배열로 저장한다.
        *   **하이트맵:** uint16 리틀엔디안, row-major 순서 (`[z=0,x=0], [z=0,x=1], ... [z=63,x=63]`), 8,192바이트/타일
        *   **스플랫맵:** uint8 RGBA, row-major 순서, 16,384바이트/타일 (64x64x4)
    *   네트워크 전송 시 gzip 압축 적용 (평탄한 지형은 높은 압축률).
    *   **좌표계:** 월드 중앙을 원점 (0, 0)으로 하는 부호 있는 타일 좌표를 사용한다.
        *   중앙 지역부터 콘텐츠를 제작하고 사방으로 점진적 확장이 가능하다.
        *   32km 월드 기준 타일 좌표 범위: -250 ~ +249 (각 축)
    *   **파일 구조:** 타일당 1파일, 리전 디렉토리(16x16 타일 = 1km x 1km)로 분류.
        ```
        data/terrain/
          height/
            r+00_+00/              ← 리전 디렉토리 (원점 근처)
              h_+0000_+0000.bin    ← 타일 (월드 좌표 x=0, z=0)
              h_+0001_+0000.bin
              ...
            r-01_+00/              ← 음수 방향 리전
              h_-0012_+0005.bin
              ...
          splat/
            r+00_+00/
              s_+0000_+0000.bin
              ...
          meta/
            r+00_+00.json          ← 리전 메타데이터 (텍스처 구성, tile scale)
            ...
          minimap/
            r+00_+00.png           ← 리전 미니맵 이미지
            ...
        ```
        *   파일명은 부호 있는 월드 타일 좌표를 사용 (`h_{±x}_{±z}.bin`).
        *   리전당 최대 256개 파일, 리전 디렉토리 수 약 1,000개 (32km 기준).
    *   **네트워크 API:**
        *   타일 데이터는 REST API로 제공한다. HTTP 캐싱(ETag, Cache-Control) 및 CDN 배포에 유리하다.
            *   `GET/PUT /api/terrain/height/{±x}/{±z}` → 하이트맵 바이너리 (gzip)
            *   `GET/PUT /api/terrain/splat/{±x}/{±z}` → 스플랫맵 바이너리 (gzip)
            *   `GET/PUT /api/terrain/meta/{±rx}/{±rz}` → 리전 메타데이터 JSON
            *   `GET/PUT /api/terrain/minimap/{±rx}/{±rz}` → 미니맵 PNG
            *   `GET /api/terrain/walk/{±x}/{±z}` → 이동 플래그 맵 바이너리
            *   `GET /api/terrain/objects/{±x}/{±z}` → 타일 오브젝트 JSON
            *   `GET /api/terrain/object_defs` → 오브젝트 타입 카탈로그 JSON
            *   `GET /api/terrain/worldmap/{±rx}/{±rz}` → 월드맵 PNG
            *   `DELETE /api/terrain/region/{±rx}/{±rz}` → 리전 전체 삭제
        *   *(TODO)* 타일 변경 알림은 WebSocket으로 전달하고, 수신한 클라이언트는 REST로 최신 데이터를 다시 받는다.

## 5. 절차적 지형 생성 (Procedural Terrain Generation)

지형 데이터를 수동으로 한 셀씩 편집하는 것은 비현실적이므로, 리전(16×16 타일 = 1,024×1,024 셀) 단위로 절차적 생성을 지원한다. 생성은 클라이언트에서 수행하고, 결과를 서버에 저장한다.

*   **실제 지형 데이터 도입 시도:** 초기에 [Tangram Heightmapper](https://tangrams.github.io/heightmapper/)에서 실제 지형의 하이트맵을 추출하여 게임에 변환·도입하는 방식을 시도했으나, 원본 데이터의 노이즈가 심해 지형 품질이 낮았다. 이에 실제 지형 데이터를 직접 사용하지 않고, 절차적 생성으로 전환했다.
    *   변환 도구 `tools/import_heightmap.py`가 남아 있다. 그레이스케일 PNG(0=최저, 255=최고)를 게임의 65×65 uint16 타일 형식으로 분할·변환한다. 높이 범위, m/px 스케일, 원점 타일 좌표 등을 인자로 지정할 수 있다. 사용 예:
        ```bash
        uv run --with Pillow --with numpy tools/import_heightmap.py \
            heightmap.png --min-height 0 --max-height 300 \
            --meters-per-pixel 1.0 --origin-tile 0 0 --terrain-dir data/terrain
        ```

### 5.1 생성 파이프라인 (6-Phase Pipeline)

전체 생성 과정은 6단계로 구성되며, 리전 하나(1,048,576 셀)를 약 3~4초에 생성·저장한다.

**Phase 1: 기반 고도 생성 (fBm Simplex Noise)**
*   6-옥타브 fBm(Fractional Brownian Motion) 심플렉스 노이즈로 기반 고도를 생성한다.
    *   심플렉스 노이즈는 연속적이고 자연스러운 랜덤 값을 생성하는 그래디언트 노이즈 함수이다. fBm은 이 노이즈를 여러 주파수(옥타브)로 겹쳐 합산하여, 큰 지형 윤곽(저주파)과 세밀한 요철(고주파)을 동시에 표현한다. 옥타브가 높을수록 디테일이 증가한다.
*   파라미터: Lacunarity 2.0, Persistence 0.5, 기본 주파수 1/512.
    *   Lacunarity: 옥타브마다 주파수를 몇 배로 높일지 (2.0 = 매 옥타브마다 2배 세밀해짐).
    *   Persistence: 옥타브마다 진폭을 몇 배로 줄일지 (0.5 = 고주파일수록 영향이 절반씩 감소).
    *   기본 주파수: 첫 옥타브의 노이즈 스케일 (1/512 = 약 512m 주기의 큰 지형 윤곽).
*   월드 좌표 기반으로 노이즈를 샘플링하므로, 같은 시드를 사용하면 리전 간 경계가 자연스럽게 연속된다.

**Phase 2: 분위수 기반 바이옴 분류 및 높이 재매핑 (Quantile Classification)**
*   Phase 1에서 생성된 노이즈 값은 임의의 범위를 가지므로, 이를 게임에 적합한 높이로 변환해야 한다.
*   전체 1,048,576개의 노이즈 값을 정렬하여 분위수(percentile) 임곗값을 구한다. 예를 들어 바다 30%, 평원 40%, 산 30%으로 설정하면, 정렬된 값에서 하위 30% 지점과 하위 70% 지점이 바이옴 경계가 된다.
*   각 셀의 노이즈 값이 어느 분위수 구간에 속하는지에 따라 4개 바이옴으로 분류하고, 해당 바이옴의 목표 높이 범위로 선형 보간(lerp)하여 최종 높이를 결정한다:
    *   **심해(Deep Sea):** 높이 범위 \[-500m, -1m\]
    *   **얕은 바다(Shallow Sea):** 높이 범위 \[-1m, -0.1m\]
    *   **평원(Plains):** 높이 범위 \[0.5m, 10m\]
    *   **산악(Mountains):** 높이 범위 \[10m, maxHeight\]
*   노이즈 값의 상대적 순위가 보존되므로 지형의 자연스러운 기복이 유지되면서, 분위수 방식 덕분에 사용자가 지정한 바이옴 비율이 정확히 반영된다.

**Phase 3: 강 조각 (River Carving)**
*   산악 봉우리(높이 > 15m)에서 출발하여 경사 하강(gradient descent)으로 해수면까지 강을 조각한다.
*   20% 확률로 무작위 횡방향 드리프트를 추가하여 자연스러운 곡류를 표현한다.
*   강 폭 2~3셀, 가우시안 단면으로 깊이 변화를 준다. 채널 깊이는 해수면 아래(-2m ~ -0.5m).

**Phase 4: 해안선 평활화 (Coastline Smoothing)**
*   바다 셀에서 BFS(Breadth-First Search, 너비 우선 탐색) 홍수 채움(flood-fill)으로 해안 거리 필드를 계산한다.
    *   모든 바다 셀(높이 < 0)을 거리 0으로 시작하여 BFS로 인접 셀을 탐색하고, 각 육지 셀이 해안선에서 얼마나 떨어져 있는지를 계산한다. 이 거리 필드는 해안선 스무딩과 스플랫맵 생성(해안 근처 모래 텍스처 배치 등)에 활용된다.
*   해안 근처(8~40셀)에 가변 폭 가우시안 블러를 적용하여 부드러운 해안선 전환을 만든다.
*   저주파 노이즈로 위치별 해안 폭을 변동시켜 단조로움을 방지한다.

**Phase 5: 리전 경계 블렌딩 (Region Boundary Blending)**
*   인접 리전의 경계 높이 데이터를 서버에서 가져온다.
*   각 변의 16셀 밴드에서 선형 보간(linear interpolation)으로 기존 리전과 이음새 없이 연결한다.
*   인접 리전이 없으면 이 단계를 건너뛴다.

**Phase 6: 스플랫맵 자동 페인팅 (Splat Map Auto-Painting)**
*   셀별로 높이, 경사도, 해안 거리, 수중 여부를 기반으로 4채널 스플랫맵을 자동 생성한다.
    *   **R (풀):** 평지, 해안에서 먼 육지
    *   **G (바위):** 급경사면
    *   **B (모래):** 해안 근처
    *   **A (눈):** 고고도 지역
*   채널별 가중치는 255로 정규화한다.

### 5.2 생성 설정 파라미터

| 파라미터 | 범위 | 기본값 | 설명 |
|---------|------|-------|------|
| `seed` | 정수 | 랜덤 | 노이즈 시드 (동일 시드 → 동일 결과) |
| `minHeight` | -500 ~ 0m | -20m | 최저 고도 (심해 깊이) |
| `maxHeight` | 1 ~ 3,276m | 80m | 최고 고도 (산 정상) |
| `seaProportion` | 0 ~ 0.6 | 0.3 | 바다 비율 (30%) |
| `plainProportion` | 0 ~ 0.8 | 0.5 | 평원 비율 (50%) |
| `mountainProportion` | 0 ~ 0.6 | 0.2 | 산악 비율 (20%) |
| `shallowSeaRatio` | 0 ~ 1 | 0.3 | 바다 중 얕은 바다 비율 |
| `riverCount` | 0 ~ 5 | 2 | 강 개수 |
| `referenceImage` | (선택) | 없음 | 리퍼런스 이미지 (5.4절 참조) |

### 5.3 생성 UI (GenerateTerrainDialog)

월드맵 다이얼로그에서 현재 리전에 대해 지형 생성 다이얼로그를 열 수 있다. 위 표의 파라미터를 슬라이더와 입력 필드로 조절한다.

*   **리퍼런스 이미지:** 업로드, 미리보기(64×64px), 이미지 크기(px/m) 표시, 제거 버튼. 사용 시 바이옴 비율 슬라이더는 비활성화된다.
*   **진행 표시:** 단계별 프로그레스 바 (인접 데이터 로드 → 생성 → 메타 저장 → 타일 저장 → 미니맵 생성).
*   다이얼로그 값은 닫았다 다시 열어도 유지된다.

### 5.4 리퍼런스 이미지 기반 생성 (Reference Image-Guided Generation)

사용자가 업로드한 리퍼런스 이미지를 기반으로 바이옴 분포를 제어할 수 있다. 절차적 노이즈가 세부 지형의 자연스러운 기복을 담당하는 반면, 리퍼런스 이미지는 "이 지역은 바다, 저 지역은 산" 같은 전체 월드의 거시적 지형 배치를 지정하는 역할이다.

**이미지 규격:**
*   1픽셀 = 32m × 32m (월드 좌표 기준).
*   이미지 중심 = 월드 원점 (0, 0).
*   예: 1,024×1,024px 이미지 → 32,768m × 32,768m (32×32 리전) 영역을 커버.

**바이옴 컬러 코드:**

| 색상 | RGB 값 | 바이옴 |
|------|--------|-------|
| 파란색 | (1, 42, 254) | 바다 (Sea) |
| 초록색 | (7, 247, 46) | 평원 (Plains) |
| 갈색 | (127, 79, 39) | 산악 (Mountains) |
| 흰색 | (253, 255, 254) | 고원/눈 (Highland/Snow) |
| 하늘색 | (60, 232, 194) | 강 (River) |

*   색상은 정확히 일치하지 않아도 된다. 각 픽셀의 RGB 값과 위 바이옴 색상들 간의 거리를 계산하여 가장 가까운 바이옴의 가중치가 높아지므로, 대략적인 색상으로도 의도한 바이옴 분포를 표현할 수 있다.

**샘플링 알고리즘:**
1.  각 셀의 월드 좌표를 이미지 픽셀 좌표로 변환한다.
2.  쌍선형 보간(bilinear interpolation)으로 서브픽셀 정밀도의 색상을 샘플링한다.
3.  샘플링된 색상과 5개 바이옴 컬러의 역제곱 거리(inverse-squared distance) 가중치를 계산하여 바이옴 혼합 비율을 결정한다.
4.  이미지 가장자리 5픽셀(160m)에서 페이드아웃하여 이미지 밖 영역과 자연스럽게 전환한다.
5.  이미지 범위 밖의 셀은 Phase 2의 분위수 기반 분류로 폴백한다.

**현재 사용 중인 리퍼런스 이미지:**

* 손으로 그린 세계 지형 리퍼런스 이미지 ![map1.png](images/map1.png)
* Gemini에게 줘서 자연스럽게 변경해 달라고 함 ![map2.png](images/map2.png)

### 5.5 핵심 파일

| 파일 | 역할 |
|------|------|
| `client/src/lib/terrain/terrainGenerator.ts` | 6-Phase 생성 파이프라인 전체 (734줄) |
| `client/src/lib/utils/simplex-noise.ts` | 2D 심플렉스 노이즈, fBm, 시드 RNG (147줄) |
| `client/src/lib/terrain/referenceImageSampler.ts` | 리퍼런스 이미지 로드·샘플링 (140줄) |
| `client/src/lib/terrain/regionMinimapGenerator.ts` | 미니맵 PNG 생성 (165줄) |
| `client/src/lib/components/map-editor/GenerateTerrainDialog.svelte` | 생성 UI 다이얼로그 |

## 6. 월드맵 시각화 (World Map Visualization)

### 6.1 미니맵 이미지 생성

서버에 저장된 하이트맵·스플랫맵 데이터를 기반으로 리전 단위 미니맵 이미지를 클라이언트에서 생성한다.

**생성 과정:**
1.  리전 내 256개 타일(16×16)의 하이트맵·스플랫맵을 서버에서 배치 로드한다 (16개씩 묶어 요청).
2.  각 셀(1m)의 색상을 결정한다:
    *   **심해** (높이 < -1.5m): RGB(30, 60, 150)
    *   **얕은 바다** (-1.5m ≤ 높이 < -0.4m): RGB(100, 160, 220)
    *   **육지:** 스플랫맵의 지배적 채널에 해당하는 텍스처 대표색을 사용
        *   R (풀): RGB(80, 140, 50)
        *   G (자갈): RGB(160, 150, 130)
        *   B (모래): RGB(194, 178, 128)
        *   A (눈): RGB(240, 240, 245)
3.  1,024×1,024px 캔버스에 렌더링하여 PNG로 인코딩한다.
4.  `PUT /api/terrain/minimap/{rx}/{rz}`로 서버에 업로드한다.

**생성 타이밍:**
*   지형 생성(절차적 생성) 완료 시 자동으로 해당 리전의 미니맵을 재생성한다.
*   에디터에서 타일을 수동 편집·저장할 때도 재생성 가능.

### 6.2 월드맵 다이얼로그 (WorldMapDialog)

캔버스 기반의 인터랙티브 월드맵 뷰어.

*   **드래그 이동:** 좌클릭 드래그로 카메라 팬.
*   **줌:** 마우스 휠로 줌 인/아웃 (1~32 리전 범위).
*   **텔레포트:** Ctrl+Click으로 해당 월드 좌표로 즉시 이동 (디버그/에디터 모드).
*   **지형 생성/삭제:** 디버그 모드에서 플레이어가 서 있는 리전에 대해 지형 생성(Gen) 및 삭제(Del) 버튼이 표시된다.
*   **플레이어 표시:** 현재 위치를 빨간 원 + 글로우로 표시.
*   **상태 유지:** 줌 레벨과 카메라 위치는 다이얼로그를 닫았다 다시 열어도 유지된다.
*   **이미지 캐싱:** 로드된 미니맵 이미지를 모듈 레벨 캐시에 저장하여 중복 로드를 방지한다. 미니맵 갱신 시 캐시 버전을 증가시켜 무효화한다.

### 6.3 저장

*   파일: `data/terrain/minimap/wm_{±rx}_{±rz}.png`
*   파일: `data/terrain/worldmap/wm_{±rx}_{±rz}.png`

### 6.4 탐험 기반 공개 (Fog of War) *(TODO)*

*   월드맵은 처음부터 전체가 공개되지 않는다. 플레이어별로 공개된 리전 집합을 서버가 관리한다.
*   공개 방법:
    *   NPC에서 지도 아이템 구매 → 특정 리전(들) 공개.
    *   선택적: 플레이어가 리전에 진입하면 해당 리전 자동 공개.
*   클라이언트는 공개된 리전의 월드맵 이미지만 표시하고, 미공개 리전은 안개 처리한다.
*   현재 위치 마커는 항상 표시. 줌 인/아웃 및 스크롤 지원.

## 7. 지형 물리 및 이동 제한 (Terrain Physics) *(TODO)*

*   **경사 제한:** 인접 셀 간 높이 차이가 1.0m (45도)를 초과하면 이동 불가로 판정한다.
    *   판정 기준: `abs(nextHeight - currentHeight) > 1.0`
    *   완만한 경사와 계단은 통과 가능, 절벽은 차단.
*   **경사면 이동 속도:**
    *   경사에 따라 셀 이동 시간을 조절한다. 오르막은 느리게, 내리막은 빠르게.
    *   속도 배율: `speedMultiplier = 1.0 - 0.5 * (heightDiff / 1.0)` (heightDiff = 다음 셀 높이 - 현재 셀 높이)
        *   오르막 (+1.0m): 0.5배, 평지 (0m): 1.0배, 내리막 (-1.0m): 1.5배 (또는 1.0 캡)
    *   서버 검증: 그리드 기반 이산 이동이므로, 셀 전환 간격(시간)이 예상 범위 내인지만 확인한다 (네트워크 지연 허용 마진 포함).
*   **이동 플래그 맵 (Walkability Map):**
    *   셀당 1바이트, 4비트로 방향별 이동 차단을 표현한다 (N/E/S/W 각 1비트).
    *   에디터에서 건물, 성벽 등 오브젝트를 배치하면 해당 셀들이 자동으로 이동 불가로 마킹된다.
    *   타일당 64 x 64 = 4,096바이트. 하이트맵/스플랫맵과 동일한 구조로 저장.
        *   파일: `data/terrain/walk/r+00_+00/w_+0000_+0000.bin`
    *   서버는 이 그리드 플래그만으로 이동 검증 및 패스파인딩(A*)을 수행한다 — 3D 충돌 판정 불필요.
    *   **다층 구조물의 이동 판정:** 이동 플래그 맵은 기본 지형(ground) 레이어만 담당한다.
        *   다리, 2층 등 다층 오브젝트는 자체 메타데이터로 이동 가능 영역과 높이를 정의한다.
        *   서버는 플레이어의 현재 레이어를 추적하여 ground → 이동 플래그 맵, 오브젝트 레이어 → 오브젝트 메타데이터로 각각 검증한다.
        *   NetHack 스타일의 랜덤 생성 다층 던전이 다수 존재할 수 있다. 던전 내부는 월드 하이트맵과 독립된 자체 맵 데이터(층별 이동 플래그, 높이, 오브젝트 배치)를 사용한다.
*   **서버 측 검증:** 경사 제한과 이동 플래그를 모두 서버가 판단한다 (치트 방지). 클라이언트는 시각적 피드백만 담당.
    *   **위반 대응:** 서버는 잘못된 이동을 즉시 거부하고 마지막 유효 위치를 클라이언트에 강제 전송한다 (보정).
        *   네트워크 지연, 클라이언트 버그 등 정상적인 원인이 있을 수 있으므로 즉시 밴하지 않는다.
        *   위반 횟수를 카운터로 누적하고 시간 경과에 따라 감쇠시킨다.
        *   단계적 대응: 보정만 → 경고 메시지 → 임시 킥 → 로그 기록 후 운영자 수동 검토.
*   **이동 프로토콜 (Movement Protocol):**
    *   **논리적 위치 (서버):** 셀 좌표 `(cellX, cellZ)` 정수 + 마지막 이동 시각.
    *   **시각적 위치 (클라이언트):** 자유로운 `(x, y, z)` 부동소수점. 셀 사이를 보간하여 부드러운 이동을 표현한다.
    *   **흐름:**
        1.  클라이언트에서 캐릭터가 셀 경계를 넘는 순간, `move(direction)` 패킷 전송 (N/E/S/W, 1바이트).
        2.  서버가 셀 단위로 검증 (이동 플래그, 경사, 속도 타이밍).
        3.  승인 시 서버의 논리적 위치 갱신 → 주변 플레이어에게 브로드캐스트.
        4.  클라이언트는 승인된 셀 간을 보간(interpolation)하여 3D 위치를 부드럽게 갱신. Y좌표는 하이트맵에서 보간.
    *   전투, 아이템 획득 등 게임플레이 판정은 모두 셀 좌표 기준으로 처리한다.

## 8. 성능 목표 (Performance Targets)

*   **프레임 레이트:**
    *   **권장 사양:** 60fps (RTX 4060, RX 7600 급)
    *   **최소 사양:** 30fps (GTX 1060, Intel Iris Xe 급)
*   **렌더링 버짓 (60fps = 16.6ms/프레임):**
    *   **지형:** 9타일 x ~8K 삼각형 = ~72K 삼각형, draw call 9회. 전체 버짓의 소량.
    *   **오브젝트:** 주요 비용. 동일 타입 오브젝트는 인스턴싱(Instancing)으로 draw call 최소화.
    *   **캐릭터:** 화면 내 동시 렌더 수 제한 (예: 최대 50명, 나머지는 네임택만 표시).
*   **낮은 사양 대응:**
    *   장식 오브젝트 밀도 조절 (잔디 등 절차적 데코 감소/제거).
    *   그림자 품질 및 해상도 단계 조절.
    *   캐릭터 동시 렌더 수 축소.

## 9. 향후 확장 계획

*   **LOD (Level of Detail):** 원거리 지형을 위한 저해상도 메쉬 시스템.
*   **수계 시스템 (Water System):** 바다는 구현 완료. 상세는 [WATER_SYSTEM.md](WATER_SYSTEM.md) 참조. 강, 호수는 향후 확장.
*   **동적 환경:** 시간대 및 날씨 변화에 따른 지형 텍스처/조명 동기화.
